# Legacy backup

Questa cartella contiene file vecchi non più caricati da `index.html`.

## File spostati qui

- `table-zoom.js` — prima versione dello zoom tabella.
- `table-zoom-v2.js` — seconda versione dello zoom tabella.

## File legacy rimossi dalla root

- `home-ui.js` — vecchio file unico che accorpava sezioni, pannello richieste, menu alto e archivio.

`home-ui.js` è stato sostituito da moduli più puliti:

- `sections-ui.js`
- `requests-panel-ui.js`
- `top-menu-ui.js`

Il vecchio `home-ui.js` resta recuperabile dalla cronologia Git, ma non viene più caricato dalla pagina.
